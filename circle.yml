machine:
  environment:
    CC_TEST_REPORTER_ID: 83083ac6c418843a38fb6371b01a734ebf274777dd0595fa05b0880a3fdc01bb

dependencies:
  post:
    - curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
    - chmod +x ./cc-test-reporter

test:
  pre:
    - ./cc-test-reporter before-build

  post:
    - ./cc-test-reporter after-build --debug --exit-code $EXIT_CODE
